{
  "name": "shared",
  "version": "0.1.0",
  "private": true,
  "description": "Tipos e utilitários compartilhados entre apps",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc",
    "lint": "eslint \"src/**/*.ts\"",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "typescript": "^5.3.0"
  }
}